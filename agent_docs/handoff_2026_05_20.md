# Handoff — 2026-05-20 15:xx CLT

Sesión previa de Claude Code (dentro de Antigravity) cerró Sprint A2 #6 a las 14:13. Después Antigravity se actualizó y dejó de funcionar, así que se reinicia Claude Code desde fuera del IDE.

Este doc es el primer archivo que debe leer la próxima sesión.

---

## Contexto operativo

- **Carpeta de trabajo activa:** `C:\dev\Aerial-Anatomy-Project` (donde estás ahora).
- **Las dos copias stale fueron archivadas en zips el 20-may:**
  - `C:\Users\alsal\OneDrive\Desktop\Aerial-Anatomy-Archive-Documentos-2026-05-20.zip` (11.27 MB, 629 entries, sin node_modules / .expo / dist)
  - `C:\Users\alsal\OneDrive\Desktop\Aerial-Anatomy-Archive-DesktopDevelop-2026-05-20.zip` (11.56 MB, 803 entries, sin node_modules / .expo / dist)
- **`OneDrive\Documentos\Aerial-Anatomy-Project` fue eliminado** tras verificar el zip.
- **`OneDrive\Desktop\Develop\Aerial-Anatomy-Project` queda pendiente** — era el cwd de la sesión que generó este handoff y no se puede borrar desde adentro. El usuario lo elimina manualmente desde Explorer/PowerShell.

## Snapshot git al cierre de la sesión previa

```
HEAD = 5bebfd31cb6072389eb748f4a94976ac1bd925af
branch main, +45 commits ahead of origin/main, sin rebase pendiente
```

Working tree (resumen — confirmar con `git status -sb` real):

**Modificados (M):**
- `App.tsx`, `CLAUDE.md`, `TODO.md`, `package.json`, `package-lock.json`
- `agent_docs/followups/dead_code.md`, `agent_docs/followups/pending.md`
- `src/components/body/Anatomy3DScene.tsx`
- `src/components/body/Anatomy3DViewer.tsx`
- `src/components/ui/ErrorBoundary.tsx`
- `src/data/anatomy3DAssets.ts`, `src/data/muscles.ts`
- `src/i18n/locales/{en,es}.ts`
- `src/screens/CuerpoScreen.tsx`
- `src/store/useAppStore.ts`
- `tools/batch-build-models.mjs`
- ~38 GLBs bajo `assets/3d-models/*.glb`

**Deletions (D) — cleanup 2D ya hecho:**
- `src/components/body/BodyMap.tsx`
- `src/components/body/MuscleTooltip.tsx`
- `src/components/body/ViewModeToggle.tsx`
- `src/components/body/ZoomableBody.tsx`

**Untracked (??):**
- `agent_docs/junta_2026_05_14.md`
- `assets/3d-models/m_escalenos.glb`
- `src/components/body/Anatomy3DTutorial.tsx`
- `src/components/body/MuscleInfoCard.tsx`
- `src/services/crashReporter.ts`
- `tools/decimate-with-gltfpack.mjs`

## Qué cerró la sesión previa (Sprint A2 #6 — crash reporting shim)

Última acción 14:13:04 del 20-may: shim de crash reporter sin agregar dependencia Sentry.

- `src/services/crashReporter.ts` (nuevo, 4.4 KB) — API estilo Sentry: `initCrashReporter`, `captureException`, `captureMessage`, `setUser`, `isRemoteReportingActive`. Tipos `CrashSeverity`, `CrashContext`, `CrashUser`. Console-log por ahora; cuando se agregue DSN, se reescribe el archivo y los callers no cambian.
- `App.tsx` — llama `initCrashReporter()` al boot (antes de fonts/auth/RC) y `setCrashUser({ id })` desde el callback de auth.
- `src/components/ui/ErrorBoundary.tsx` — `componentDidCatch` ahora reporta vía `captureException` con `source: 'ErrorBoundary'` y `componentStack` como extra.
- `agent_docs/followups/pending.md` — sección "Sprint A2 follow-ups" con los 6 pasos exactos para enchufar Sentry real cuando se decida.

Última verificación del chat anterior: `tsc --noEmit exit 0`. **Re-verificar** antes de commitear.

## Plan de ejecución que viene

### 0. Verificación previa al primer commit

```powershell
cd C:\dev\Aerial-Anatomy-Project
git fetch origin
git status -sb
$env:NODE_OPTIONS = '--max-old-space-size=4096'
npx tsc --noEmit
npm test
```

Si `tsc` o `npm test` fallan → NO commitear, diagnosticar primero. Si pasan → seguir.

### 1. Split de commits propuesto (ajustable tras leer diff)

Orden sugerido (atómicos, mensaje en formato `area(scope): descripción`):

1. **`feat(crash): add Sentry-compatible shim + wire init/captureException`**
   - `src/services/crashReporter.ts` (nuevo)
   - `App.tsx`
   - `src/components/ui/ErrorBoundary.tsx`

2. **`feat(3d): tutorial overlay + haptics + cinematic setViewOffset easing`**
   - `src/components/body/Anatomy3DTutorial.tsx` (nuevo)
   - `src/components/body/Anatomy3DScene.tsx`
   - `src/components/body/Anatomy3DViewer.tsx`
   - i18n keys nuevas en `src/i18n/locales/{es,en}.ts` (solo el bloque `tutorial3D`)

3. **`feat(3d): muscle info card popover`**
   - `src/components/body/MuscleInfoCard.tsx` (nuevo)
   - Cualquier wiring en `CuerpoScreen.tsx` específico al MuscleInfoCard

4. **`feat(data): activate 4 additional muscles in 3D registry`**
   - `src/data/anatomy3DAssets.ts`

5. **`chore(cleanup): remove unused getMusclesByRegion + 2D dead components`**
   - `src/data/muscles.ts` (delete `getMusclesByRegion`)
   - `D src/components/body/BodyMap.tsx`
   - `D src/components/body/MuscleTooltip.tsx`
   - `D src/components/body/ViewModeToggle.tsx`
   - `D src/components/body/ZoomableBody.tsx`
   - `agent_docs/followups/dead_code.md` (sync con lo realmente borrado)
   - **Confirmar con grep** que nadie los importa antes de cerrar este commit.

6. **`chore(3d-assets): regenerate 38 muscle GLBs (sub-mesh decimation pipeline)`**
   - `assets/3d-models/*.glb` (38 binarios, ~1-2 MB cada uno)
   - `tools/batch-build-models.mjs`, `tools/decimate-with-gltfpack.mjs`
   - **Revisar tamaño total** antes de pushear. Si suma >50-100 MB, evaluar Git LFS antes.

7. **`chore(deps): update package.json + lockfile`**
   - `package.json`, `package-lock.json`
   - Solo si los cambios de deps son intencionales (revisar diff). Si solo es lockfile churn, puede ir junto con otro commit.

8. **`chore(store): adjust useAppStore for tutorial flag / crash user wiring`**
   - `src/store/useAppStore.ts`
   - Depende del diff real.

9. **`docs: junta 2026-05-14 + Sprint A2 follow-ups + TODO/CLAUDE.md update`**
   - `agent_docs/junta_2026_05_14.md` (nuevo)
   - `agent_docs/followups/pending.md`
   - `TODO.md`
   - `CLAUDE.md`
   - Este `handoff_2026_05_20.md` también puede entrar acá o quedar fuera (es ephemera).

### 2. Push

```powershell
# Confirmar que no hay drift en origin
git fetch origin
git log origin/main..HEAD --oneline | Measure-Object -Line   # debe ser >= 45 + nuevos commits

# Push
git push origin main
```

Si hay drift en `origin/main` (alguien más comiteó), preferir `git pull --rebase origin main` antes que merge.

### 3. Próximo sprint (después del push)

**Sprint A2 #7 — Smoke test E2E con Maestro.** Detalle en `agent_docs/followups/pending.md` sección "#7". Pasos:
1. Instalar Maestro CLI (cross-platform, gratis).
2. Escribir flow YAML mínimo: boot → onboarding skip → CuerpoTab → tap muscle → InfoCard visible → cerrar → MovimientosTab → 1 movement card → detalle.
3. Correr contra simulator/device. ~15 min setup + ~30 min flow.

Otros pendientes A2 (orden por owner-voice):
- RevenueCat sandbox + paywall enforcement (Sofía)
- D-day de release (Camila, propuesta tentativa: 2026-07-15 staged CL+AR)

## Recuperación de chats previos (si hace falta contexto profundo)

- Transcript completo del 20-may en: `C:\Users\alsal\.claude\projects\c--Users-alsal-OneDrive-Documentos-Aerial-Anatomy-Project\8d312513-a8b2-4b00-8065-bb8261f011e3.jsonl` (47 MB).
- Aunque el path encoded de la carpeta dice "Documentos", todos los archivos manipulados están físicamente en `C:\dev\Aerial-Anatomy-Project`.
- No es necesario leer todo el jsonl: este handoff cubre las decisiones operativas. Solo abrirlo si hay una decisión ambigua que reconstruir.

## Plan archivado

El plan que generó este handoff: `C:\Users\alsal\.claude\plans\expressive-prancing-dewdrop.md`. Útil para auditar las decisiones de archivado y limpieza si hay dudas.
